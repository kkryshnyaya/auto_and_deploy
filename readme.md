# Автоматизация и деплой пайплайна продаж 

## 📌 Краткое описание проекта

Проект реализует автоматизированную обработку данных о продажах торговой сети.  
Настроена генерация ежедневных csv-файлов с чеками, загрузка данных в PostgreSQL, удаление обработанных файлов и ведение логов с автоматической очисткой старых записей.  
Дополнительно проект развёрнут на удалённом сервере: настроено расписание запуска через cron и подключен Metabase для проверки соединения и визуального контроля загруженных данных (шаблон дашборда).

**Стек:** Python, PostgreSQL, Metabase, cron.

---

## 🎯 Цель проекта

Разработать надёжный автоматизированный процесс ежедневной обработки данных о продажах торговой сети:
- эмулировать работу кассового ПО;
- собирать и загружать данные в БД;
- обеспечивать стабильную работу пайплайна;
- настраивать деплой и автоматический запуск на сервере.

---

## 🧩 Реализация

### 1. `logs.py`
- Настройка логирования;
- Удаление логов старше 7 дней;
- Класс `Logs` импортируется во все модули проекта.

### 2. `sales_generator.py`
- Генерация кассовых csv-файлов;
- Формирование продаж за предыдущий день;
- Отдельный файл для каждой кассы магазина;
- Уникальный идентификатор каждого чека.

### 3. `database.py`
- Объединение кассовых файлов в единый отчёт;
- Загрузка отчёта в PostgreSQL;
- Автоматическое создание таблицы при первом запуске.

### 4. `run.py`
- Главный управляющий модуль проекта;
- Импорт всех рабочих классов;
- Проверка дня недели:
  - если воскресенье — данные не генерируются, делается запись в лог;
- При наличии файлов продаж:
  - загрузка в БД;
  - удаление обработанных файлов;
- Гарантированное закрытие соединений с БД.

## 🕒 Дополнительно настроено (на удаленном сервере)

- Автоматический запуск через **crontab** (ежедневно в 09:10);
- Шаблон дашборда в **Metabase** для проверки соединения и контроля загруженных данных:

http://79.174.81.111:3000/dashboard/2-home-store.

Для просмотра Metabase и БД необходимо обратиться к разработчику проекта.

---

## 📂 Структура репозитория

### Данные и примеры
- `data_example/` — сгенерированные csv-чеки;
- `logs_example/` — логи работы пайплайна (локальный запуск).

### Скриншоты
- `img/`
  - `bd.png` — база данных;
  - `cron_remote.png` — настройка запуска по расписанию;
  - `log_remote.png` — логи;
  - `metabase_remote_example.png` — черновой дашборд в Metabase.

Файлы с суффиксом `_remote` получены на удалённом сервере.

### Основные модули
- `logs.py` — настройка логирования;
- `sales_generator.py` — генерация csv-файлов с продажами;
- `database.py` — загрузка данных в PostgreSQL;
- `run.py` — главный управляющий модуль.

### Конфигурация
- `constants.ini` — пути к директориям, параметры торговой сети, категории и перечень товаров;
- `requirements.txt` — список зависимостей Python.

### Файлы, не входящие в репозиторий
- `creds.ini` — параметры подключения к PostgreSQL (добавлен в `.gitignore`, создаётся вручную на удалённом сервере).

---

## ✅ Результаты проекта

- Реализован автоматизированный пайплайн обработки продаж;
- Настроена генерация исходных данных;
- Данные ежедневно загружаются в PostgreSQL;
- Реализовано логирование и контроль ошибок;
- Настроен деплой и автоматический запуск на удалённом сервере.

---

## 🧾 Где искать логи
Логи автоматически сохраняются в папке `logs/` и хранятся за последние 7 дней.